node_modules/
*.log
.DS_Store
Thumbs.db

# Output / generated projects
output/

# Input files (user data, not code)
input/

# Workspace/staging (temp files)
workspace/
staging/

# Bird's Nest — lives at top level for visibility but is a separate repo
# (HesKenY/CHERP-Nest). Don't track its files inside the Piper repo.
nest/

# Desktop folder (separate projects, not part of Pipe-R)
Desktop/

# Google Sheets sync secrets
agent_mode/sheets/token.json
agent_mode/sheets/config.json
agent_mode/sheets/credentials.json
