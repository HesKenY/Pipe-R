# ── Core server ────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
PyYAML>=6.0.1
aiofiles>=23.2.1
python-multipart>=0.0.9

# ── Desktop automation (Mode 2+) ───────────────────────
# Required for capture_screen, move_mouse, click, type_text, hotkey
pyautogui>=0.9.54
Pillow>=10.0.0
pygetwindow>=0.0.9

# ── Optional: OCR for HUD reading ──────────────────────
# Required by halo_vision_hunt.py workflow. Needs tesseract binary
# installed separately — https://github.com/UB-Mannheim/tesseract
# pytesseract>=0.3.10

# ── Optional: richer search / formatting ──────────────
# Only needed if running the Python project lint/format drills
# black>=24.0
# flake8>=7.0
# mypy>=1.8
